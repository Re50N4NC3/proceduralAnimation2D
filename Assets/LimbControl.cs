using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LimbControl : MonoBehaviour { 
    // UNUSED
    /*
    float speed = 2.2f;
    public Transform currentTarget;

    // Update is called once per frame
    void Update() {
        SetLimbPosition();
    }

    void SetLimbPosition() {
        gameObject.transform.position = Vector2.MoveTowards(transform.position, currentTarget.position, speed * Time.deltaTime);
    }
    */
}
